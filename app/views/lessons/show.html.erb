<%= link_to 'Courses', courses_path, class: "btn" %>
/
<%= link_to @lesson.course.title, course_path(@lesson.course), class: "btn" %>
/
<%= link_to @lesson.title, course_lesson_path(@lesson) %>

<%= render 'lessons/lesson', lesson: @lesson %>

<%= link_to 'Back to course', course_path(@lesson.course) %>
